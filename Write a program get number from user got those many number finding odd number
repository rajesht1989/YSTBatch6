#include <stdio.h>
int getNumberFromUser(){
    int i;
    scanf("%d", &i);
    return i;
}
void oddNumber(int a){
    int numberofodd=0;
    for(int i=0;i<a;i++){
        printf("Enter a number\n ",i+1);
    int c= getNumberFromUser();
       if(c % 2 != 0)numberofodd++;
       }
printf("odd number is %d", numberofodd);
}

void main(){
    printf("enter a  iteration ");
    int a=getNumberFromUser();
    oddNumber(a);
